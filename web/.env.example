# Scavengers Environment Variables
# Copy this file to .env.local and fill in your values

# ===========================================
# REQUIRED FOR PRODUCTION
# ===========================================

# JWT Secret - Generate a secure random string (min 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=

# ===========================================
# SUPABASE DATABASE (via Vercel Integration)
# ===========================================
# When you connect Supabase to Vercel, these are auto-configured:
#
# 1. Go to Vercel Dashboard → Your Project → Storage
# 2. Click "Connect Store" → Select "Supabase"
# 3. Create or link your Supabase project
# 4. The POSTGRES_URL will be automatically set
#
# For local dev, get the connection string from:
# Supabase Dashboard → Project Settings → Database → Connection string (URI)
POSTGRES_URL=

# ===========================================
# OPTIONAL
# ===========================================

# Google Gemini API Key (enables AI hunt generation)
# Without this, the app will use mock data for AI generation
# Get yours at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# App URL (set automatically in Vercel, only needed for local dev)
NEXT_PUBLIC_APP_URL=http://localhost:3000
